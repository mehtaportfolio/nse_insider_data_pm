// Legacy CSV export has been removed. This module is intentionally left as a compatibility stub.
export async function writeCSV() {
    throw new Error("CSV export is no longer supported. Use the Supabase sync flow instead.");
}